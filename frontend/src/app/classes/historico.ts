export class Historico {
    constructor(public id: string, public data_calculo: string, public peso: number, public carboidrato: number, public tipo_alimento: string, public alimento: string, public usuario: string) { }
}
